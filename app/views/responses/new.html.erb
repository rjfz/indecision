
<div>
  <ul role="list" class="mt-3 text-center grid grid-cols-1 lg:grid-cols-4 gap-6"  >
    <li class="lg:col-span-4 flex shadow-sm rounded-md px-4">
      <div class="flex-1 flex items-center justify-between border border-gray-200 bg-white rounded-r-md truncate">
        <div class="flex-1 px-4 py-2 text-sm truncate">
          <a href="#" class="text-gray-900 font-medium hover:text-gray-600"><%= @response.lobby_question.question.title %></a>
          <p class="text-gray-500 text-sm"><%= @response.question.time_limit %></p>
        </div>
      </div>
    </li>
    <% @response.question.answer.each do |answer|%>
    <li class="flex shadow-sm rounded-md px-4">
      <div class="flex-1 flex items-center justify-between border border-gray-200 bg-white rounded-r-md truncate">
        <div class="flex-1 px-4 py-2 text-sm">
          <a href="#" class="text-gray-900 font-medium hover:text-gray-600">Graph API</a>
        </div>
        <div class="flex-shrink-0 pr-2">
          <button type="button" class="w-8 h-8 bg-white inline-flex items-center justify-center text-gray-400 rounded-full bg-transparent hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
            <span class="sr-only">Open options</span>
          </button>
        </div>
      </div>
      <% end %>
    </li>
